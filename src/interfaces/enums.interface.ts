export enum AttachFileType {
  Temp = 'Temp',
  Static = 'Static',
  Secure = 'Secure',
}

export enum ProductType {
  Emoticon = 'Emoticon',
  Prop = 'Prop',
}

export enum ContextType {
  Request = 'Request',
  WebSocket = 'WebSocket',
  Unknown = 'Unknown',
}

export enum UserType {
  None = 'None',
  Guest = 'Guest',
  Kakao = 'Kakao',
}

export enum Payment {
  Cash,
  Card,
}
