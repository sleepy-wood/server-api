export enum AttachFileType {
  Temp = 'Temp',
  Static = 'Static',
  Secure = 'Secure',
}

export enum ContextType {
  Request = 'Request',
  WebSocket = 'WebSocket',
  Unknown = 'Unknown',
}

export enum UserType {
  None = 'None',
  Guest = 'Guest',
  Kakao = 'Kakao',
}
